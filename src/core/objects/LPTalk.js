const LPObject = require('./LPObject')
const LPString = require('./LPString')
const LPFloat = require('./LPFloat')

module.exports = class LPTalk extends LPObject {
    tag(tags) {
        throw new Error('TODO')
    }

    isIncest() {
        throw new Error('TODO')
    }
}